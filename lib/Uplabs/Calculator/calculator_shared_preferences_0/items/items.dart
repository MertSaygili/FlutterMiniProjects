import 'package:flutter/material.dart';

class IconItems {
  final Icon iconCalculator = const Icon(Icons.calculate, color: Colors.black);
  final Icon iconFunction = const Icon(Icons.functions, color: Colors.black);
  final Icon iconHistory = const Icon(Icons.history, color: Colors.black);
}

class ColorItems {
  Color colorBlack = Colors.black;
  Color colorWhite = Colors.white;
}
